package org.yamdut.view.auth;

// Minimal stub to avoid duplicate/invalid source during conflict resolution.
public class LoginScreen {
    // Intentionally minimal: controllers use org.yamdut.view.login.LoginScreen
    // Keep this file present so older imports referencing view.auth compile.
}

